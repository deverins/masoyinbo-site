import React from 'react';
import ParticipationForm from './Participate';

export const metadata = {
  title: 'Participation | Form',
  description: 'This is the participant form page.',
};

const Page = () => {
  return <ParticipationForm />;
};

export default Page;
