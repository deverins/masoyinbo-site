import React from 'react';
import ParticipationUsers from './Users';

export const metadata = {
  title: 'Partcipation | Users',
  description: 'This is the participation users page.',
};

const Page = () => {
  return <ParticipationUsers />;
};

export default Page;
