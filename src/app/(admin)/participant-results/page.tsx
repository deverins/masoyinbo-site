import React from 'react';
import ParticipantResultPage from './Participant';

export const metadata = {
  title: 'Participant-results',
  description: 'This is the participant results page.',
};

const Page = () => {
  return <ParticipantResultPage />;
};

export default Page;
